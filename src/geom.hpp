#pragma once

#define _USE_MATH_DEFINES
#include <math.h>


namespace geom
{
    struct vect2_t
    {
        double x, y;
    };

    struct vect3_t
    {   
        double x, y, z;
    };
} // namespace geom
