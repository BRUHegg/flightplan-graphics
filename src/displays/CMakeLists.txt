file(GLOB_RECURSE DISPLAY_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

add_library(displays STATIC ${DISPLAY_SRC})


if(UNIX AND NOT APPLE)
    set_property(TARGET displays PROPERTY POSITION_INDEPENDENT_CODE ON)
endif()
